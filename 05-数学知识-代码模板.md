# AcWing 算法基础课 - 数学知识代码模板

> 来源：https://www.acwing.com/blog/content/406/

## 试除法判定质数 —— AcWing 866

```cpp
bool is_prime(int x)
{
    if (x < 2) return false;
    for (int i = 2; i <= x / i; i++)
        if (x % i == 0) return false;
    return true;
}
```

## 试除法分解质因数 —— AcWing 867

```cpp
void divide(int x)
{
    for (int i = 2; i <= x / i; i++)
        if (x % i == 0)
        {
            int s = 0;
            while (x % i == 0) x /= i, s++;
            cout << i << ' ' << s << endl;
        }
    if (x > 1) cout << x << ' ' << 1 << endl;
    cout << endl;
}
```

## 朴素筛法求素数 —— AcWing 868

```cpp
int primes[N], cnt;
bool st[N];

void get_primes(int n)
{
    for (int i = 2; i <= n; i++)
    {
        if (st[i]) continue;
        primes[cnt++] = i;
        for (int j = i + i; j <= n; j += i)
            st[j] = true;
    }
}
```

## 线性筛法求素数 —— AcWing 868

```cpp
int primes[N], cnt;
bool st[N];

void get_primes(int n)
{
    for (int i = 2; i <= n; i++)
    {
        if (!st[i]) primes[cnt++] = i;
        for (int j = 0; primes[j] <= n / i; j++)
        {
            st[primes[j] * i] = true;
            if (i % primes[j] == 0) break;
        }
    }
}
```

## 试除法求约数 —— AcWing 869

```cpp
vector<int> get_divisors(int x)
{
    vector<int> res;
    for (int i = 1; i <= x / i; i++)
        if (x % i == 0)
        {
            res.push_back(i);
            if (i != x / i) res.push_back(x / i);
        }
    sort(res.begin(), res.end());
    return res;
}
```

## 约数个数与约数之和 —— AcWing 870, 871

```
N = p1^c1 * p2^c2 * ... * pk^ck
约数个数 = (c1+1) * (c2+1) * ... * (ck+1)
约数之和 = (p1^0 + ... + p1^c1) * ... * (pk^0 + ... + pk^ck)
```

## 欧几里得算法 (GCD) —— AcWing 872

```cpp
int gcd(int a, int b)
{
    return b ? gcd(b, a % b) : a;
}
```

## 欧拉函数 —— AcWing 873

```cpp
int phi(int x)
{
    int res = x;
    for (int i = 2; i <= x / i; i++)
        if (x % i == 0)
        {
            res = res / i * (i - 1);
            while (x % i == 0) x /= i;
        }
    if (x > 1) res = res / x * (x - 1);
    return res;
}
```

## 筛法求欧拉函数 —— AcWing 874

```cpp
int primes[N], cnt;
int euler[N];
bool st[N];

void get_eulers(int n)
{
    euler[1] = 1;
    for (int i = 2; i <= n; i++)
    {
        if (!st[i])
        {
            primes[cnt++] = i;
            euler[i] = i - 1;
        }
        for (int j = 0; primes[j] <= n / i; j++)
        {
            int t = primes[j] * i;
            st[t] = true;
            if (i % primes[j] == 0)
            {
                euler[t] = euler[i] * primes[j];
                break;
            }
            euler[t] = euler[i] * (primes[j] - 1);
        }
    }
}
```

## 快速幂 —— AcWing 875

```cpp
int qmi(int m, int k, int p)
{
    int res = 1 % p, t = m;
    while (k)
    {
        if (k & 1) res = res * t % p;
        t = t * t % p;
        k >>= 1;
    }
    return res;
}
```

## 扩展欧几里得算法 —— AcWing 877

```cpp
int exgcd(int a, int b, int &x, int &y)
{
    if (!b)
    {
        x = 1; y = 0;
        return a;
    }
    int d = exgcd(b, a % b, y, x);
    y -= (a / b) * x;
    return d;
}
```

## 高斯消元 —— AcWing 883

```cpp
int gauss()
{
    int c, r;
    for (c = 0, r = 0; c < n; c++)
    {
        int t = r;
        for (int i = r; i < n; i++)
            if (fabs(a[i][c]) > fabs(a[t][c])) t = i;

        if (fabs(a[t][c]) < eps) continue;

        for (int i = c; i <= n; i++) swap(a[t][i], a[r][i]);
        for (int i = n; i >= c; i--) a[r][i] /= a[r][c];
        for (int i = r + 1; i < n; i++)
            if (fabs(a[i][c]) > eps)
                for (int j = n; j >= c; j--)
                    a[i][j] -= a[r][j] * a[i][c];
        r++;
    }

    if (r < n)
    {
        for (int i = r; i < n; i++)
            if (fabs(a[i][n]) > eps) return 2; // 无解
        return 1; // 无穷多组解
    }

    for (int i = n - 1; i >= 0; i--)
        for (int j = i + 1; j < n; j++)
            a[i][n] -= a[i][j] * a[j][n];

    return 0; // 唯一解
}
```

## 组合数

### 递推法 —— AcWing 885 (a,b ≤ 2000)

```cpp
for (int i = 0; i < N; i++)
    for (int j = 0; j <= i; j++)
        if (!j) c[i][j] = 1;
        else c[i][j] = (c[i-1][j] + c[i-1][j-1]) % mod;
```

### 预处理逆元 —— AcWing 886 (a,b ≤ 10^5)

```cpp
fact[0] = infact[0] = 1;
for (int i = 1; i < N; i++)
{
    fact[i] = (LL)fact[i-1] * i % mod;
    infact[i] = (LL)infact[i-1] * qmi(i, mod-2, mod) % mod;
}
// C(a,b) = fact[a] * infact[b] % mod * infact[a-b] % mod
```

### Lucas 定理 —— AcWing 887 (a,b 很大, p 较小质数)

```cpp
int lucas(LL a, LL b, int p)
{
    if (a < p && b < p) return C(a, b, p);
    return (LL)C(a % p, b % p, p) * lucas(a / p, b / p, p) % p;
}
```

## 卡特兰数 —— AcWing 889

```
Cat(n) = C(2n, n) / (n + 1)
```

## NIM 博弈 —— AcWing 891

```
先手必胜 ⟺ A1 ^ A2 ^ ... ^ An != 0
```

## SG 函数 —— AcWing 893

```
SG(x) = mex({SG(y1), SG(y2), ..., SG(yk)})
多个游戏的 SG 值 = 各子游戏 SG 值的异或和
必胜 ⟺ SG > 0
```
